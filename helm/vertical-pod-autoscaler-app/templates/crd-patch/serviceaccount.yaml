apiVersion: v1
kind: ServiceAccount
metadata:
  name: vpa-annotate-job-sa
  labels:
    {{- include "vertical-pod-autoscaler.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
